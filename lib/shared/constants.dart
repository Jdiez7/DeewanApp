import 'package:flutter/material.dart';

const textInputDecoration = InputDecoration(
    fillColor: Colors.white,
    filled: true,
    enabledBorder: OutlineInputBorder(
      borderSide: BorderSide(color: Colors.white, width: 1.0),
    ),
    focusedBorder: OutlineInputBorder(
      borderSide: BorderSide(color: Colors.black, width: 1.0),
    )
);

const globalColor1 = Color(0xdfbc8c3c);
const globalColor2 = Colors.black;
const globalColor3 = Colors.white;
const globalTextColor = Colors.black;
const appBarColor = Color(0xff222222);
const backgroundOpacity = 0.4;
const backgroundColor = Colors.black;
const globalTileColor = Color(0x9fffffff);